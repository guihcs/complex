IntersectionOf(
    PaperPresentation,
    Restriction(
        onProperty=relatedToPaper,
        someValuesFrom=Restriction(
            onProperty=InverseOf(hasRelatedDocument),
            someValuesFrom=Conference
        )
    )
)