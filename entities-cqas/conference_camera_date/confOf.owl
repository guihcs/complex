


Restriction(
    onProperty = InverseOf(ends_on),
    someValuesFrom = IntersectionOf(
        Camera_Ready_event,
        Restriction(
            onProperty = InverseOf(hasAdministrativeEvent),
            someValuesFrom = Conference
        )
    )
)