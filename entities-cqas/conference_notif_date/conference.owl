

Restriction(
    onProperty = InverseOf(is_a_date_of_acceptance_announcement),
    someValuesFrom = IntersectionOf(
        Important_dates,
        Restriction(
            onProperty = InverseOf(has_important_dates),
            someValuesFrom = Conference_volume
        )
    )
)