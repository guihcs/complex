SELECT distinct ?s ?o WHERE {  
?s <http://cmt#title> ?o .  
?s a <http://cmt#PaperAbstract> .
 }


IntersectionOf(
    PaperAbstract,
    Restriction(
        onProperty=title,
        someValuesFrom=string
    )
)